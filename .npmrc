# Pin this repo to the PUBLIC npm registry. All devDependencies are public OSS, so CI
# (GitHub Actions) and external contributors must resolve from npmjs — not a private/
# corporate registry that needs auth (which is what broke `npm ci` in CI with E401).
# Contains NO credentials.
registry=https://registry.npmjs.org/
